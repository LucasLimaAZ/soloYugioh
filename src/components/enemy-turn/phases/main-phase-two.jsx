import { Typography, Box } from "@mui/material";

const MainPhaseTwo = () => (
  <Box sx={{ marginTop: "12px" }}>
    <Typography variant="body1">Opponent turn ends</Typography>
  </Box>
);

export default MainPhaseTwo;
