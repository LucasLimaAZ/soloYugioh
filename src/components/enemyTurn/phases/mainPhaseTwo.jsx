import { Typography, Box } from "@mui/material";

const MainPhaseTwo = () => (
  <Box sx={{ marginTop: "10px" }}>
    <Typography variant="h6">Enemy turn ends</Typography>
  </Box>
);

export default MainPhaseTwo;
